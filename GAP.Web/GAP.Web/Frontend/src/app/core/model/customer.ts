import { FullName } from './fullname';

export class Customer {
  customerId: number;
  fullName: FullName;
  email: string;
  status: number;
  birthDay: Date;
  nit: string;
  documentType: number;
}
